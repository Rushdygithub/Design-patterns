//FactoryDesignPatern is one of the most commonly used design patterns in software development.
//When we keep our programmer loosly coupled, we can use this patter
//no need to inject dependency using new keyword, we can use factory design pattern to create object and inject dependency